%%-*-latex-*-

\documentclass[10pt]{article}

\usepackage{pst-tree,pst-eps}

\begin{document}

\input{commands}

\TeXtoEPS
%\psset{showbbox=true}
\pstree[nodesep=2pt,levelsep=25pt,treesep=6pt,arrows=<-]{\TR{\(\sum_{i=q}^{r-1}{2^{e_{i}}}+2^{q}-1\)}}{
  \psset{nodesepA=0.5pt}
  \TR{\(\Bcomp(2^{e_r})\)}
  \Tn
  \psset{nodesep=0.5pt,linestyle=dotted}
  \pstree[nodesep=2pt,linestyle=solid]{\TR{\(2^{q}-1\)}}{
    \TR{\(\Bcomp(2^{e_{q}})\)}
    \pstree{\TR{\(2^{q-1}-1\)}}{
      \TR{\(\Bcomp(2^{q-1})\)}
      \psset{linestyle=dotted}
      \pstree[linestyle=solid]{\TR{\(2^{1}-1\)}}{
        \TR{\(\Bcomp(2^{1})\)}
        \Tn
        \TR{\(\Bcomp(2^{0})\)}
      }
    }
  }
}
\endTeXtoEPS

\end{document}
